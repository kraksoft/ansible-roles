---
- name: add repository
  apt_repository: >
    repo=ppa:alexmurray/indicator-sensors
    state=present
    update_cache=yes

- name: install package
  apt: >
    pkg=indicator-sensors
    state=present
