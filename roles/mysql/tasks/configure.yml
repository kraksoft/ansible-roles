---
- name: copy configs
  template: >
    dest="/etc/mysql/conf.d/{{ item }}"
    force=yes
    group=root
    mode="0644"
    owner=root
    src="{{ mysql_files_path }}{{ item }}"
  notify: restart mysql
  with_items: mysql_configs
