---
- name: add repository
  apt_repository: >
    repo="ppa:libreoffice/libreoffice-{{ libreoffice_version | replace('.', '-') }}"
    state=present
    update_cache=yes

- name: install package
  apt: >
    force=yes
    pkg=libreoffice
    state=present
