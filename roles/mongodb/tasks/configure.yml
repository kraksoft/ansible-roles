---
- name: set parameters
  lineinfile: >
    dest=/etc/mongodb.conf
    line="{{ item.key }} = {{ item.value }}"
    regexp="^(.*){{ item.key }}(\s*)="
    state=present
  notify: restart mongodb
  with_dict: mongodb_parameters
