---
- name: copy configs
  template: >
    dest="{{ item.0.home }}/.composer/config.json"
    force=yes
    group="{{ item.0.group }}"
    mode="0600"
    owner="{{ item.0.name }}"
    src="{{ composer_files_path }}{{ item.1 }}"
  with_subelements:
    - composer_users
    - configs
