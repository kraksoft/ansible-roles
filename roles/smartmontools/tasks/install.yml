---
- name: install package
  apt: >
    name=smartmontools
    state=present

- name: install post-packages
  apt: >
    name="{{ item }}"
    state=present
  with_items: "{{ smartmontools_post_packages }}"
