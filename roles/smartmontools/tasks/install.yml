---
- name: install package
  apt: >
    pkg=smartmontools
    state=present

- name: install post-packages
  apt: >
    pkg="{{ item }}"
    state=present
  with_items: "{{ smartmontools_post_packages }}"
